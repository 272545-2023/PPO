#include "ui.h"
// #include "students.h"
#include <iostream>
#include <vector>

using namespace std;

// vector<Student> students;

int main()
{
    ui();
    // addStudent(students);
    // addStudent(students);
    // printStudents(students);
    return 0;
}